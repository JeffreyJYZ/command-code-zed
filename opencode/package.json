{
	"name": "@jeffreyjyz/opencode-command-code",
	"version": "0.1.5",
	"description": "Command Code provider + usage dashboard for opencode: two providers (Anthropic wire for Claude, OpenAI wire for everything else) against api.commandcode.ai, live model list, plan gating, /cmd-usage.",
	"type": "module",
	"main": "dist/index.js",
	"types": "dist/index.d.ts",
	"scripts": {
		"build": "bun build src/index.ts --outdir dist --target bun --external @opencode-ai/plugin && tsc -p tsconfig.build.json",
		"test": "bun test",
		"typecheck": "tsc --noEmit"
	},
	"dependencies": {
		"@opencode-ai/plugin": "*"
	},
	"devDependencies": {
		"@types/bun": "latest",
		"typescript": "^5"
	},
	"files": ["dist", "LICENSE"],
	"keywords": ["opencode", "command-code", "plugin", "provider", "ai-sdk"],
	"license": "MIT",
	"publishConfig": {
		"access": "public"
	}
}
